name: CI Build

# Triggers on push and branches on the master
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  # Allows to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  all:
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        build_type : [ Debug ]
        shared_type : [ ON ]
        device: [ gpu_nvidia ]
    name: "${{matrix.build_type}} Shared=${{matrix.shared_type}} ${{matrix.device}}"
    runs-on: [ "${{matrix.device}}", self-hosted ]
    env:
      BUILD_TYPE:  ${{matrix.build_type}}
      SHARED_TYPE: ${{matrix.shared_type}}
      DEVICE: ${{matrix.device}}
      BUILD_DIRECTORY: build
      INSTALL_DIRECTORY: install
      # Job-wide: CMake's try_run probes are sanitized too, and would fail on
      # leaks alone before we ever reach the test.
      ASAN_OPTIONS: detect_leaks=0:print_stacktrace=1
      SETUP: ${{github.workspace}}/.github/workflows/env_setup.sh
    defaults:
      run:
        shell: bash

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Setup Build Environment ${{ runner.os }}
      id: setup
      # Create a separate build directory and use it for all subsequent commands
      run: |
          source $SETUP
          cmake -E make_directory ${{ env.BUILD_DIRECTORY }}

    - name: Configure CMake
      id: configure
      working-directory: ${{ env.BUILD_DIRECTORY }}
      # Note the current convention is to use the -S and -B options here to specify source 
      # and build directories, but this is only available with CMake 3.13 and higher.  
      # The CMake binaries on the Github Actions machines are (as of this writing) 3.12
      run: |
        source $SETUP
        CC=gcc CXX=g++ FC=gfortran cmake $GITHUB_WORKSPACE $BUILD_CONFIG
        CC=gcc CXX=g++ FC=gfortran cmake $GITHUB_WORKSPACE -DCMAKE_BUILD_TYPE=$BUILD_TYPE $BUILD_CONFIG

    - name: Build
      working-directory: ${{ env.BUILD_DIRECTORY }}
      run: |
        source $SETUP
        cmake --build .

    - name: Install
      working-directory: ${{ env.BUILD_DIRECTORY }}
      run: |
          source $SETUP
          cmake --build . --target install

    - name: Describe the machine
      working-directory: ${{ env.BUILD_DIRECTORY }}
      # Results here only mean something alongside the host they ran on: the
      # runners differ by an order of magnitude in core count, and the failure
      # we are chasing has only ever appeared on the large ones. Also confirm
      # the sanitizer really is in the binary before trusting a clean run.
      run: |
        echo "runner=${{ runner.name }} host=$(hostname) nproc=$(nproc)"
        nm tests/testing_dgetrf_1d 2>/dev/null | grep -c asan_init || true
        ldd tests/testing_dgetrf_1d | grep -i asan || echo "no libasan (static or absent)"

    - name: Test
      working-directory: ${{ env.BUILD_DIRECTORY }}
      # Investigation only. dgetrf_1d_mpi reproduces reliably here and fails
      # silently, with no assertion tripped, so run it under AddressSanitizer
      # and let it report the access rather than the wrong answer it leads to.
      # Leak detection is off: parsec does not free everything at exit and the
      # reports would bury the one we are after.
      run: |
        source $SETUP
        ctest --output-on-failure -R 'dgetrf_1d_mpi' --repeat until-fail:40

    - name: Save Artifact
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: CMake-error-log
        path: ${{ env.BUILD_DIRECTORY }}/CMakeFiles/CMakeError.log
